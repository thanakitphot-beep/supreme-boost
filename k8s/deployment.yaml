apiVersion: apps/v1
kind: Deployment
metadata:
  name: omega-jarvis
  labels:
    app: omega-jarvis
spec:
  replicas: 3
  selector:
    matchLabels:
      app: omega-jarvis
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: omega-jarvis
    spec:
      containers:
      - name: omega-jarvis
        image: your-registry/omega-jarvis:3.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: omega-jarvis-secrets
        - configMapRef:
            name: omega-jarvis-config
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
