apiVersion: v1
kind: Service
metadata:
  name: omega-jarvis-service
  labels:
    app: omega-jarvis
spec:
  type: ClusterIP
  selector:
    app: omega-jarvis
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
