# INDICATOR release — 2026-09-11

- Local contracts: 190/190 passed, including widget build, bounded entity context, private-file serving restrictions and fine-tuned model routing.
- MCP stdio smoke: initialization, 3 tools, preview, context and curriculum audit passed.
- Training preparation: 72 train / 24 validation / 24 holdout, synthetic only.
- Actual OpenAI training attempt stopped at billing preflight: HTTP 429 `credit_balance_exhausted`. No training files uploaded and no fine-tuning job created.
- Commercial API comparison: Qwen `qwen/qwen3.8-27b` via Groq completed 15 requests: 12 passed keyword/action checks, 1 answer failed checks, 2 rate-limited. OpenAI exhausted credits; Gemini had 1 successful answer then rate limits. These results do not establish parity with commercial agents.
- Computer Use now imports `@oai/sky` and reads/operates the VS Code window. Screen capture returned `SetIsBorderRequired failed: No such interface supported (0x80004002)`. VS Code remains in Restricted Mode until the user grants folder trust. No standalone Computer Use MCP bridge was created.
- Deployment target: existing Render service `indicator-web-chat`, Singapore, free plan. Deploy the exact tested commit; use Render's existing environment and database configuration.

Reproduce: `npm test`, `npm test --prefix tools/indicator-mcp`, `node evals/fine-tune-openai.js` (offline plan), `node evals/compare-market.js --live --groq-only` (uses API quota).

Once API credits are available: `node evals/fine-tune-openai.js start`, then `node evals/fine-tune-openai.js status`. Review validation and held-out performance before selecting the returned `ft:...` model for production.
